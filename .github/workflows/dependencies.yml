name: Dependency health

on:
  schedule:
    - cron: "19 6 * * 1"
  workflow_dispatch:

permissions:
  contents: read
  id-token: write
  pull-requests: read

jobs:
  update:
    uses: rvben/upd/.github/workflows/dependency-health.yml@493e20ad77a95c5b2efcc88581886491356e1a1b # v0.8.7
    with:
      runner: ubuntu-24.04
      upd-version: v0.8.7
      upd-target: x86_64-unknown-linux-gnu
      upd-sha256: d48f5252383aff235e32b0b04530f07beb9237c62dec0a784451edcd8022f14e
      langs: ""
      lock: true
      min-age: 7d
      max-bump: minor
      prepare-command: >-
        rustup toolchain install stable --profile minimal --component clippy --component rustfmt &&
        rustup toolchain install 1.82.0 --profile minimal &&
        rustup default stable
      validation-command: make dependency-check
      validate-actions: true
      fail-on-blocked: false
      branch: deps/upd
      commit-message: "chore(deps): update dependencies with upd"
      auto-merge: false
